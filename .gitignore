__pycache__/
*.pyc
*.pyo

# Python virtualenvs (never commit — includes bundled binaries like Playwright)
.venv/
venv/

# home_menu runtime outputs (poller JSON/DB in data/, unrotated logs in logs/)
home_menu/data/
home_menu/logs/

# moisture endpoint runtime output
moisture.db
moisture_alert_state.json

# systemd units are installed copies; unifi-poller.service carries an API key
# and this repo is PUBLIC, so ignore them all — except the secret-free menu unit.
*.service
!home_menu/home-menu.service

# scratch / one-off analysis files
handoff.md
HANDOFF.md
dump_*.py
plot_*.py
end_result.txt
bbc_news.png

# Local wireless lab — captures (crackable handshakes/PMKID), wordlists,
# vendored tools, and recon notes. Never publish to the public repo.
wireless-lab/

# hcpy — vendored third-party Home Connect tool with its own .git
hcpy/

# Sibling project repos — each has its own git remote, not part of this repo
How_hot_is_harold/
bad_parents/
bbc_spoofer/
next_train/
now_playing/
watched_pot/
weather/
whats_darren_doing/
homelab_mcp/

# local-only, not part of this repo
task_tracker/

# MCP client config — bakes in a machine-specific absolute path
.mcp.json

# packet captures — may contain plaintext LAN traffic, never publish
gateway-pcap-*.tar.gz
